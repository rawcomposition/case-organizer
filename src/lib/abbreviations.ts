// Approved medical abbreviations and their expansions. The left value is the
// abbreviation as it should appear; the right value is the full expansion.
export const approvedAbbreviations: [string, string][] = [
  ["2D", "2-dimensional"],
  ["3D", "3-dimensional"],
  ["17-OHP", "17-hydroxyprogesterone"],
  ["aCGH", "Array comparative genomic hybridization"],
  ["ACTH", "Adrenocorticotropic hormone"],
  ["AFI", "Amniotic fluid index"],
  ["AFP", "Alpha-fetoprotein"],
  ["AGC", "Atypical glandular cells"],
  ["AIS", "Adenocarcinoma in situ"],
  ["ALT", "Alanine aminotransaminase"],
  ["AMA", "Advanced maternal age"],
  ["AMH", "Antimullerian hormone"],
  ["ANC", "Absolute neutrophil count"],
  ["APS", "Antiphospholipid antibody syndrome"],
  ["ARDS", "Acute respiratory distress syndrome"],
  ["AROM", "Artificial rupture of membranes"],
  ["ART", "Antiretroviral therapy or Assisted reproductive technology"],
  ["ASA score", "American Society of Anesthesiologists score"],
  ["ASC", "Abdominal sacrocolpopexy"],
  ["ASCUS", "Atypical cells of undetermined significance"],
  ["ASRM", "American Society for Reproductive Medicine"],
  ["AST", "Aspartate aminotransaminase"],
  ["ATFP", "Arcus tendineus fascia pelvis"],
  ["AUB", "Abnormal uterine bleeding"],
  ["AZF", "Azoospermia factor"],
  ["BEP", "Bleomycin, etoposide, cisplatin"],
  ["BSO", "Bilateral salpingo-oophorectomy"],
  ["BTL", "Bilateral tubal ligation"],
  ["BMI", "Body mass index"],
  ["BUN", "Blood urea nitrogen"],
  ["Cm", "Centimeter"],
  ["CA125", "Cancer antigen 125"],
  ["CBAVD", "Congenital bilateral absence of the vas deferens"],
  ["CBC", "Complete blood count"],
  ["CD4", "Cluster of differentiation 4"],
  ["CEA", "Carcinoembryonic antigen"],
  ["CI", "Confidence interval"],
  ["CIN", "Cervical intraepithelial neoplasia"],
  ["CMV", "Cytomegalovirus"],
  ["CNS", "Central nervous system"],
  ["COC", "Combined oral contraceptive"],
  ["CPR", "Cardiopulmonary resuscitation"],
  ["CT", "Computerized tomography"],
  ["CTA", "Computerized tomography angiography"],
  ["CTLA-4", "Cytotoxic T lymphocyte-associated antigen 4"],
  ["CVS", "Chorionic villus sampling"],
  ["dMMR", "Deficient mismatch repair"],
  ["D & C", "Dilatation and curettage"],
  ["D & E", "Dilatation and evacuation"],
  ["DEXA", "Dual-energy x-ray absorptiometry"],
  ["DHEA", "Dehydroepiandrosterone"],
  ["DHEAS", "Dehydroepiandrosterone sulfate"],
  ["DIC", "Disseminated intravascular coagulopathy"],
  ["DKA", "Diabetic ketoacidosis"],
  ["DM", "Diabetes mellitus"],
  ["DMSO", "Dimethyl sulfoxide"],
  ["DNA", "Deoxyribonucleic acid"],
  ["DSD", "Differences of sexual development"],
  ["DVP", "Deepest vertical pocket"],
  ["DVT", "Deep vein thrombosis"],
  ["EAS", "External anal sphincter"],
  ["EBL", "Estimated blood loss"],
  ["ECC", "Endocervical curettage"],
  ["ECMO", "Extracorporeal membrane oxygenation"],
  ["EGA", "Estimated gestational age"],
  ["EIN", "Endometrial intraepithelial neoplasia"],
  ["ELISA", "Enzyme-linked immunosorbent assay"],
  ["EKG/ECG", "Electrocardiogram"],
  ["EMA-CO", "Etoposide, methotrexate, actinomycin D-cyclophosphamide, Oncovin®"],
  ["EMB", "Endometrial biopsy"],
  ["EFW", "Estimated fetal weight"],
  ["ER", "Estrogen receptor"],
  ["ERAS", "Enhanced recovery after surgery"],
  ["ESHRE", "European Society of Human Reproduction and Embryology"],
  ["FDA", "Food and Drug Administration"],
  ["FENa", "Fractional excretion of sodium"],
  ["FFP", "Fresh frozen plasma"],
  ["FGR", "Fetal growth restriction"],
  ["FHR", "Fetal heart rate"],
  ["FHT", "Fetal heart tones"],
  ["FIGO", "International Federation of Gynecology and Obstetrics"],
  ["FISH", "Fluorescence in situ hybridization"],
  ["FSH", "Follicle-stimulating hormone"],
  ["g", "Gram"],
  ["GBS", "Group B streptococcus"],
  ["G-CSF", "Granulocyte colony-stimulating factor"],
  ["GDM", "Gestational diabetes mellitus"],
  ["GIFT", "Gamete intrafallopian transfer"],
  ["GnRH", "Gonadotropin-releasing hormone"],
  ["GOG", "Gynecologic Oncology Group"],
  ["GTD", "Gestational trophoblastic disease"],
  ["GTN", "Gestational trophoblastic neoplasia"],
  ["HbA1c", "Hemoglobin A1c"],
  ["HELLP", "Hemolysis, elevated liver function tests, low platelet count"],
  ["HCG", "Human chorionic gonadotropin"],
  ["HIV", "Human immunodeficiency virus"],
  ["hMG", "Human menopausal gonadotropin"],
  ["HNPCC", "Hereditary nonpolyposis colorectal cancer"],
  ["HPO", "Hypothalamic-pituitary-ovarian"],
  ["HPV", "Human papillomavirus"],
  ["HRT", "Hormone replacement therapy"],
  ["HSG", "Hysterosalpingogram"],
  ["HSIL", "High-grade squamous intraepithelial lesion"],
  ["HSV", "Herpes simplex virus"],
  ["IAS", "Internal anal sphincter"],
  ["IC/BPS", "Interstitial cystitis/Bladder pain syndrome"],
  ["ICSI", "Intracytoplasmic sperm injection"],
  ["ICU", "Intensive care unit"],
  ["IgG", "Immunoglobulin G"],
  ["IgM", "Immunoglobulin M"],
  ["IM", "Intramuscular"],
  ["IMB", "Intermenstrual bleeding"],
  ["INR", "International normalized ratio"],
  ["IPG", "Implantable pulse generator"],
  ["IUD", "Intrauterine device"],
  ["IUFD", "Intrauterine fetal death"],
  ["IUI", "Intrauterine insemination"],
  ["IUP", "Intrauterine pregnancy"],
  ["IV", "Intravenous"],
  ["IVF", "In vitro fertilization"],
  ["IVIG", "Intravenous immunoglobulin"],
  ["kg", "Kilogram"],
  ["KUB", "Kidney, ureter, bladder"],
  ["L & D", "Labor and delivery"],
  ["LARC", "Long-acting reversible contraception"],
  ["LAVH", "Laparoscopic-assisted vaginal hysterectomy"],
  ["LDH", "Lactate dehydrogenase"],
  ["LEEP", "Loop electrosurgical excision procedure"],
  ["LGA", "Large for gestational age"],
  ["LGBTQIA", "Lesbian gay bisexual transgender queer intersex asexual"],
  ["LFT", "Liver function test"],
  ["LH", "Luteinizing hormone"],
  ["LMP", "Last menstrual period"],
  ["LMWH", "Low-molecular-weight heparin"],
  ["LSIL", "Low-grade squamous intraepithelial lesion"],
  ["LVSI", "Lymphovascular space invasion"],
  ["mL", "Milliliter"],
  ["mTOR", "Mammalian target of rapamycin"],
  ["MCA", "Middle cerebral artery"],
  ["MESA", "Microsurgical epididymal sperm aspiration"],
  ["MIS", "Minimally invasive surgery"],
  ["MRI", "Magnetic resonance imaging"],
  ["MRKH", "Mayer-Rokitansky-Küster-Hauser"],
  ["MSAFP", "Maternal serum alpha-fetoprotein"],
  ["MSI-H, -L", "Microsatellite instability-high, -low"],
  ["MTP", "Massive transfusion protocol"],
  ["MURCS", "Müllerian duct aplasia, renal aplasia, cervicothoracic somite dysplasia"],
  ["NAAT", "Nucleic-acid amplification test"],
  ["NGS", "Next-generation sequencing"],
  ["NICU", "Neonatal intensive care unit"],
  ["NIPT", "Noninvasive prenatal testing"],
  ["NPO", "Nil per os"],
  ["NSAID", "Nonsteroidal anti-inflammatory drug"],
  ["OAB", "Overactive bladder"],
  ["OASIS", "Obstetric anal sphincter injuries"],
  ["OHSS", "Ovarian hyperstimulation syndrome"],
  ["OHVIRA", "Obstructed hemivagina ipsilateral renal agenesis"],
  ["PACU", "Postanesthesia care unit"],
  ["PALND", "Para-aortic lymph node dissection"],
  ["PAP", "Papanicolaou smear"],
  ["PARP", "Poly adenosine diphosphate-ribose polymerase"],
  ["PCOS", "Polycystic ovarian syndrome"],
  ["PCR", "Polymerase chain reaction"],
  ["PD-1", "Programmed cell death protein 1"],
  ["PD-L1", "Programmed cell death ligand 1"],
  ["PESA", "Percutaneous epididymal sperm aspiration"],
  ["PET", "Positron emission tomography"],
  ["PFMT", "Pelvic floor muscle therapy"],
  ["PFPT", "Pelvic floor physical therapy"],
  ["PGT-A", "Preimplantation genetic testing for aneuploidy"],
  ["PGT-M", "Preimplantation genetic testing for monogenic disorder"],
  ["PGT-SR", "Preimplantation genetic testing for structural rearrangements"],
  ["PLND", "Pelvic lymph node dissection"],
  ["PNE", "Peripheral nerve evaluation"],
  ["POP", "Pelvic organ prolapse"],
  ["POP-Q", "Pelvic organ prolapse quantification system"],
  ["PPH", "Postpartum hemorrhage"],
  ["PR", "Progesterone receptor"],
  ["PROM", "Premature rupture of membranes"],
  ["PT", "Prothrombin time"],
  ["PTT", "Partial thromboplastin time"],
  ["PPROM", "Preterm premature rupture of membranes"],
  ["PTNS", "Posterior tibial nerve stimulation"],
  ["PUBS", "Percutaneous umbilical blood sampling"],
  ["PUPPP", "Pruritic urticarial papules and plaques of pregnancy"],
  ["PVR", "Postvoid residual"],
  ["RAIR", "Rectoanal inhibitory reflex"],
  ["RBC", "Red blood cell"],
  ["RCT", "Randomized controlled trial"],
  ["RNA", "Ribonucleic acid"],
  ["RPL", "Recurrent pregnancy loss"],
  ["RPR", "Rapid plasma reagin"],
  ["SBO", "Small bowel obstruction"],
  ["S/D (ratio)", "Systolic/diastolic ratio"],
  ["SGA", "Small for gestational age"],
  ["SHBG", "Sex hormone-binding globulin"],
  ["SLND", "Sentinel lymph node dissection"],
  ["SNM", "Sacral neuromodulation"],
  ["SNP", "Single-nucleotide polymorphism"],
  ["SO", "Salpingo-oophorectomy: preceded by R (right) or L (left) or unilateral (U)"],
  ["SROM", "Spontaneous rupture of membranes"],
  ["SSLF", "Sacrospinous ligament fixation"],
  ["STI", "Sexually transmitted infection"],
  ["SUI", "Stress urinary incontinence"],
  ["SS-A", "Sjogren syndrome A"],
  ["SS-B", "Sjogren syndrome B"],
  ["SVD", "Spontaneous vaginal delivery"],
  ["T1DM", "Type I diabetes mellitus"],
  ["T2DM", "Type II diabetes mellitus"],
  ["TAH", "Total abdominal hysterectomy"],
  ["TCGA", "The Cancer Genome Atlas"],
  ["TESA", "Testicular sperm aspiration"],
  ["TESE", "Testicular sperm extraction"],
  ["TLH", "Total laparoscopic hysterectomy"],
  ["TNF", "Tumor necrosis factor"],
  ["TOLAC", "Trial of labor after cesarean"],
  ["TOT", "Transobturator tape"],
  ["TSH", "Thyroid-stimulating hormone"],
  ["TRALI", "Transfusion-related acute lung injury"],
  ["TTTS", "Twin-twin transfusion syndrome"],
  ["TUNEL", "Terminal deoxynucleotidyl transferase-mediated deoxyuridine triphosphate-nick end labelling"],
  ["TVH", "Total vaginal hysterectomy"],
  ["TVS", "Transvaginal sonography"],
  ["TVT", "Tension-free vaginal tape"],
  ["UAE", "Uterine artery embolization"],
  ["USLF", "Uterosacral ligament fixation"],
  ["UTI", "Urinary tract infection"],
  ["VAC", "Vincristine, actinomycin-D, cyclophosphamide"],
  ["VAIN", "Vaginal intraepithelial neoplasia"],
  ["VBAC", "Vaginal birth after cesarean delivery"],
  ["VCUG", "Voiding cystourethrography"],
  ["VDRL", "Venereal disease research laboratory"],
  ["VEGF", "Vascular endothelial growth factor"],
  ["VIN", "Vulvar intraepithelial neoplasia"],
  ["VLPP", "Valsalva leak point pressure"],
  ["V/Q", "Ventilation/Perfusion"],
  ["VTE", "Venous thromboembolism"],
  ["VVF", "Vesicovaginal fistula"],
  ["WBC", "White blood cell"],
  ["WES", "Whole exome sequencing"],
  ["WHO", "World Health Organization"],
  ["ZIFT", "Zygote intrafallopian transfer"],
  // Assumed abbreviations — added without confirmation against the source list.
  ["HMB", "Heavy menstrual bleeding"],
  ["POD", "Post operative day"],
  ["ED", "Emergency department"],
];

// Abbreviations that are frequently written but are NOT on the approved list,
// each mapped to what should be used instead. Drives a "Fix" button that swaps
// the written form for the replacement. `wrong` is matched as a whole-word,
// case-insensitive acronym token; `replacement` is inserted verbatim — it may be
// an approved abbreviation or a spelled-out term.
export const abbreviationCorrections: { wrong: string; replacement: string }[] = [
  ["USLS", "USLF"],
  ["TVUS", "TVS"],
  ["OCP", "COC"],
  ["BS", "bilateral salpingectomy"],
].map(([wrong, replacement]) => ({ wrong, replacement }));

// Common misspellings of approved expansions, mapped to what should be used
// instead — the approved abbreviation when one exists.
export const expansionMisspellings: { wrong: string; correct: string }[] = [
  ["dilation and curettage", "D & C"],
].map(([wrong, correct]) => ({ wrong, correct }));

export type AbbrStatus = "red" | "yellow" | null;

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

// Normalize an abbreviation to a comparison key: uppercase, alphanumerics only.
// e.g. "D & C" -> "DC", "PGT-A" -> "PGTA".
function normalizeAbbr(s: string): string {
  return s.toUpperCase().replace(/[^A-Z0-9]/g, "");
}

// Build the set of "approved" abbreviation keys. Several entries pack multiple
// variants (e.g. "EKG/ECG", "ASA score", "MSI-H, -L"), so split on slashes,
// commas and whitespace and register each piece that has at least two letters.
const APPROVED_KEYS: Set<string> = (() => {
  const keys = new Set<string>();
  for (const [abbr] of approvedAbbreviations) {
    for (const part of abbr.split(/[\s/,]+/)) {
      const key = normalizeAbbr(part);
      if ((key.match(/[A-Z]/g)?.length ?? 0) >= 2) keys.add(key);
    }
  }
  return keys;
})();

// Pre-compiled expansion matchers, used to spot a long form that could have
// been abbreviated. Case-insensitive, word-boundaried so "Gram" doesn't fire
// inside "program".
const EXPANSIONS: { abbr: string; abbrKey: string; expansionText: string; expansion: RegExp }[] =
  approvedAbbreviations.map(([abbr, expansion]) => ({
    abbr,
    abbrKey: normalizeAbbr(abbr),
    expansionText: expansion,
    expansion: new RegExp(`\\b${escapeRegExp(expansion)}\\b`, "i"),
  }));

// Lookup from a normalized unapproved abbreviation to its replacement.
const CORRECTIONS: Map<string, string> = new Map(
  abbreviationCorrections.map(({ wrong, replacement }) => [normalizeAbbr(wrong), replacement])
);

// Pre-compiled matchers for misspelled expansions. Case-insensitive and
// word-boundaried so they only fire on the whole phrase.
const MISSPELLINGS: { wrong: string; correct: string; re: RegExp }[] =
  expansionMisspellings.map(({ wrong, correct }) => ({
    wrong,
    correct,
    re: new RegExp(`\\b${escapeRegExp(wrong)}\\b`, "i"),
  }));

// Tokens we treat as acronym candidates: all-uppercase runs (letters, digits,
// hyphens) such as "TVH" or "PGT-A". Capitalized words like "Antepartum" have
// lowercase letters and are skipped.
const TOKEN_RE = /\b[A-Z0-9][A-Z0-9-]*\b/g;
// Roman numerals ("II", "III", "IV") show up constantly as grades/types and
// would otherwise look like unknown acronyms.
const ROMAN_RE = /^[IVXLCDM]+$/;

function letterCount(s: string): number {
  return s.match(/[A-Z]/g)?.length ?? 0;
}

// Returns "red" if the text uses an abbreviation that isn't on the approved
// list, "yellow" if it spells out a term that has an approved abbreviation but
// doesn't use the abbreviation, or null if neither. Red takes precedence.
export function analyzeAbbreviations(text: string): AbbrStatus {
  if (!text) return null;

  // Collect the all-caps acronym tokens (>= 2 letters) actually used in the text.
  const used = new Set<string>();
  for (const match of text.matchAll(TOKEN_RE)) {
    const token = match[0];
    if (ROMAN_RE.test(token)) continue;
    const key = normalizeAbbr(token);
    if (letterCount(key) >= 2) used.add(key);
  }

  // Red: any used acronym that isn't on the approved list.
  for (const key of used) {
    if (!APPROVED_KEYS.has(key)) return "red";
  }

  // Red: a misspelled expansion (e.g. "dilation and curettage").
  for (const { re } of MISSPELLINGS) {
    if (re.test(text)) return "red";
  }

  // Yellow: a spelled-out expansion is present but its abbreviation isn't used.
  for (const { abbrKey, expansion } of EXPANSIONS) {
    if (used.has(abbrKey)) continue;
    if (expansion.test(text)) return "yellow";
  }

  return null;
}

export interface AbbrIssue {
  // "unapproved": an abbreviation used that isn't on the approved list.
  // "opportunity": a spelled-out term that has an approved abbreviation.
  // "correction": an unapproved abbreviation that has a known replacement.
  // "misspelling": a misspelled expansion that has a known correct spelling.
  type: "unapproved" | "opportunity" | "correction" | "misspelling";
  // For "opportunity", the approved abbreviation to use; otherwise the text as
  // written (the abbreviation or phrase being flagged).
  term: string;
  // The spelled-out expansion (opportunity only).
  expansion?: string;
  // What "term" should be replaced with — the value the Fix button swaps in
  // (correction and misspelling only).
  replacement?: string;
}

// Detailed version of analyzeAbbreviations: lists every problematic
// abbreviation in the text so it can be shown to the user. Deduplicated.
export function getAbbreviationIssues(text: string): AbbrIssue[] {
  if (!text) return [];

  const issues: AbbrIssue[] = [];
  const used = new Set<string>();
  const seenUnapproved = new Set<string>();

  for (const match of text.matchAll(TOKEN_RE)) {
    const token = match[0];
    if (ROMAN_RE.test(token)) continue;
    const key = normalizeAbbr(token);
    if (letterCount(key) < 2) continue;
    used.add(key);
    if (APPROVED_KEYS.has(key) || seenUnapproved.has(key)) continue;
    seenUnapproved.add(key);
    const replacement = CORRECTIONS.get(key);
    if (replacement) {
      issues.push({ type: "correction", term: token, replacement });
    } else {
      issues.push({ type: "unapproved", term: token });
    }
  }

  for (const { abbr, abbrKey, expansionText, expansion } of EXPANSIONS) {
    if (used.has(abbrKey)) continue;
    if (expansion.test(text)) {
      issues.push({ type: "opportunity", term: abbr, expansion: expansionText });
    }
  }

  for (const { wrong, correct, re } of MISSPELLINGS) {
    if (re.test(text)) {
      issues.push({ type: "misspelling", term: wrong, replacement: correct });
    }
  }

  return issues;
}

// Replaces every (case-insensitive, whole-word) occurrence of an expansion with
// its approved abbreviation — the swap behind the "Fix" button.
export function applyAbbreviation(text: string, expansion: string, abbr: string): string {
  const re = new RegExp(`\\b${escapeRegExp(expansion)}\\b`, "gi");
  return text.replace(re, abbr);
}
